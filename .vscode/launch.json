{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "lldb bin/main",
            "type": "lldb-dap",
            "request": "launch",
            "program": "${workspaceFolder}/.cppmake/bin/main",
            "args": [
                "--compile-std=c++26",
                "--compile-type=release",
                "--link-type=dynamic",
                "--verbose"
            ],
            "env": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}